{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed:demo": "ts-node scripts/seed-demo-expanded.ts",
    "seed:demo:clean": "ts-node scripts/seed-demo-expanded.ts --clean"
  }
}
